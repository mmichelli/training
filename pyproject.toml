[project]
name = "training"
version = "0.1.0"
description = "Garmin sync + dashboard for NSA Two Oceans 2027 plan"
requires-python = ">=3.11"
dependencies = [
    "garminconnect>=0.3.2",
    "garth>=0.5.0",
]

[tool.uv]
package = false
